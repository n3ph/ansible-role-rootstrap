---

#------------------------------------------------------------------------------#
# ansible
#------------------------------------------------------------------------------#

rootstrap_ansible_authorized_keys:
 - some ssh public key
 
#------------------------------------------------------------------------------#
# root
#------------------------------------------------------------------------------#

rootstrap_password_lookup: ''
rootstrap_password_lookup_default: '' #some lookup

rootstrap_recovery_sshkey: some ssh public key